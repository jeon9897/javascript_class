제어문
1. 조건문 - 조건에 만족하는 결과를 얻고자 할때
   1) if(조건)
   2) if(조건1)  else (조건2)
   3) if(조건1)  else if(조건2)  else(조건3)

   var n=1;
   if(n==1){   //값이 같은지 비교한다.
    document.write('참');  //맞으면 결과를 출력한다.
   }

  if(n===1){
   document.write('참');
  }else{
   document.write('거짓');
  }

2. 선택문 - 정확하게 값이 일치하는 경우에만 실행
 switch(값){
 case 1:document.write('출력1');break;
 case 2:document.write('출력2');break;
 default document.write('기본값');
}

3. 반복문
 1) while
 2) do while
 3) for문